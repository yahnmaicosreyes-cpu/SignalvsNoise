# ============================================================
# requirements.txt
#
# Python dependencies for the Priority Planner web application.
# Railway reads this file and installs everything listed here
# before starting the app.
#
# Pinned to specific versions so deployments are reproducible --
# the same code will install the same dependencies tomorrow, next
# week, and next year.
# ============================================================

# Flask: the web framework. Handles routing, templates, sessions,
# request parsing, security headers, and the WSGI interface.
Flask==3.1.3

# Gunicorn: production-grade WSGI server. Flask's built-in server
# is for development only; Gunicorn is what actually runs the app
# on Railway. It handles multiple concurrent requests properly,
# restarts crashed workers, and integrates cleanly with platforms.
gunicorn==23.0.0
